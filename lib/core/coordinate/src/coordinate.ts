import { compose } from 'lib/utils'
import type { CanvasOptions, Transformer } from '../type'

export function createCoordinate(options: Partial<CanvasOptions> = {}): Transformer[] {
  // 实现坐标系创建逻辑
  return [];
}
